server:
  port: 10000
  http2:
    enabled: true
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      global-filter:
        websocket-routing:
          enabled: false
        forward-routing:
          enabled: false
        forward-path:
          enabled: false